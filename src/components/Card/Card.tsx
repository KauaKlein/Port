import React from 'react';
import { Card } from './Card'; // Verifique se o caminho está correto

export default {
  title: 'Components/Card',
  component: Card,
};

export const Default = () => <Card />;
